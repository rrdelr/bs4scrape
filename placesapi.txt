import requests
import time

api_key = 'AIzaSyCdclKQcQRdBtYm779CPufl1qxPqz6vq88'

def places(data):
    for x in data:
        # Data prep
        xlname = x["port_name"]
        xlname = xlname.replace("\n", "")
        xlname = xlname.replace(' ', '%20')

        # API Call
        x["xlname"] = xlname
        url = "https://maps.googleapis.com/maps/api/place/textsearch/xml?query=puerto%20deportivo%20" + xlname + "&key=" + api_key
        response = requests.get(url).json()
        time.sleep(1.5)
        dict.append(response)

        # Final values
        x["location"] = "placeholder"
        x["score"] = "placeholder"
        x["lat"] = "placeholder"
        x["lng"] = "placeholder"
    print(data)

    # Old unimplemented
    # for item in placenames:
    #     url = "https://maps.googleapis.com/maps/api/place/textsearch/xml?query=puerto%20deportivo%20" + item +"&key=" + api_key
    #     response = requests.get(url).json()
    #     time.sleep(1.5)
    #     dict.append(response)
    #     print(response)

# Test run
datatest = [{
    "port_name":"Port Royale"
}]
places(datatest)